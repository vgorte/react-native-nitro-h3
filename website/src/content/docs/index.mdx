---
title: react-native-nitro-h3
description: Fast H3 geospatial indexing for React Native, powered by Nitro Modules. Native H3 with bigint cells, typed-array results and batch calls.
template: splash
editUrl: false
head:
  - tag: title
    content: react-native-nitro-h3, fast H3 geospatial indexing for React Native
  - tag: meta
    attrs:
      property: og:image
      content: https://vgorte.github.io/react-native-nitro-h3/og-benchmark.png
  - tag: meta
    attrs:
      property: og:image:alt
      content: react-native-nitro-h3 against h3-js, median milliseconds per workload
hero:
  title: react-native-nitro-h3
  tagline: Fast H3 geospatial indexing for React Native, powered by Nitro Modules.
  image:
    file: ../../assets/logo-tile.svg
    alt: react-native-nitro-h3
  actions:
    - text: Get started
      link: /react-native-nitro-h3/getting-started/
      icon: right-arrow
    - text: API reference
      link: /react-native-nitro-h3/api/
      variant: minimal
---

import { Card, CardGrid, LinkCard, Steps, TabItem, Tabs } from '@astrojs/starlight/components'

<p class="badges">
  <a href="https://www.npmjs.com/package/react-native-nitro-h3"><img src="https://img.shields.io/npm/v/react-native-nitro-h3.svg" alt="npm version" /></a>
  <a href="https://github.com/vgorte/react-native-nitro-h3/actions/workflows/ci.yml"><img src="https://github.com/vgorte/react-native-nitro-h3/actions/workflows/ci.yml/badge.svg" alt="CI" /></a>
  <a href="https://github.com/vgorte/react-native-nitro-h3/blob/main/LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue.svg" alt="License: MIT" /></a>
  <a href="https://github.com/uber/h3/releases/tag/v4.5.0"><img src="https://img.shields.io/badge/h3-v4.5.0-blue.svg" alt="Vendored H3 v4.5.0" /></a>
</p>

## Up to 862× faster than h3-js

for `compactCells` on a `k=20` disk containing 1,261 cells. iPhone XS, iOS 18.7.9, React Native
0.87.0, Hermes, 20-run median, 2026-09-01. Every result was verified against `h3-js` 4.5.0 for
equivalence.

[![react-native-nitro-h3 against h3-js, median milliseconds per workload](/react-native-nitro-h3/img/benchmark.svg)](/react-native-nitro-h3/benchmark/)

## Built for the native side

<CardGrid>
  <Card title="Native execution" icon="rocket">
    H3 4.5.0 runs as compiled C++ on iOS and Android, called directly through Nitro Modules.
  </Card>
  <Card title="<code>bigint</code> cell indexes" icon="puzzle">
    H3's 64-bit indexes stay numeric. No hexadecimal strings cross the JavaScript boundary, and
    functions keep the `h3-js` names, with unit suffixes instead of a unit argument.
  </Card>
  <Card title="Typed-array results" icon="database">
    A cell set arrives as one `BigUint64Array` over the buffer C++ produced, viewed in place instead
    of copied per element.
  </Card>
  <Card title="Batch calls" icon="list-format">
    `latLngsToCells`, `cellsToLatLngs` and `cellsToBoundaries` process a whole coordinate or cell
    array in a single native call, the last answering every cell's boundary as `[lat, lng]` pairs in
    one flat buffer.
  </Card>
  <Card title="Async variants" icon="clock">
    Four async variants move the expensive calls to a background thread and resolve with the same
    typed arrays.
  </Card>
  <Card title="Optional cell ceiling" icon="padlock">
    `configure()` sets a ceiling that rejects an unexpectedly large result before allocation. Off by
    default.
  </Card>
</CardGrid>

## Install

<Tabs syncKey="package-manager">

<TabItem label="bun">

<Steps>

1. Add the package together with Nitro Modules.

   ```bash
   bun add react-native-nitro-h3 react-native-nitro-modules
   ```

2. Install the iOS pods.

   ```bash
   cd ios && pod install
   ```

3. Rebuild the app so the native module is linked, with `npx react-native run-ios` or `npx react-native run-android`.

</Steps>

</TabItem>

<TabItem label="npm">

<Steps>

1. Add the package together with Nitro Modules.

   ```bash
   npm install react-native-nitro-h3 react-native-nitro-modules
   ```

2. Install the iOS pods.

   ```bash
   cd ios && pod install
   ```

3. Rebuild the app so the native module is linked, with `npx react-native run-ios` or `npx react-native run-android`.

</Steps>

</TabItem>

<TabItem label="Expo">

<Steps>

1. Add the package with the Expo CLI.

   ```bash
   npx expo install react-native-nitro-h3 react-native-nitro-modules
   ```

2. Generate the native projects.

   ```bash
   npx expo prebuild
   ```

   Expo Go cannot load native modules. Use a development build with `npx expo run:ios` or `npx expo run:android`.

</Steps>

</TabItem>

</Tabs>

Built on the H3 C library 4.5.0 by Uber, vendored in the package and recorded in
`third_party/h3/H3_VERSION`. Every function is verified against `h3-js` 4.5.0 for equivalence in CI.

## Familiar API, native results

Functions keep the `h3-js` names, with unit suffixes instead of a unit argument. Cells are `bigint`,
cell sets are a `BigUint64Array` over native memory, and nothing is parsed on the JavaScript side.

```ts
import { gridDisk, latLngToCell } from 'react-native-nitro-h3'

const cell = latLngToCell(37.7749, -122.4194, 9) // 0x89283082803ffffn
const ring = gridDisk(cell, 1) // BigUint64Array of 7 cells
```

## Where next

<CardGrid>
  <LinkCard
    title="Getting started"
    description="Install the package and make the first call."
    href="/react-native-nitro-h3/getting-started/"
  />
  <LinkCard
    title="Showcase"
    description="One app, five acts, every cell computed on the phone."
    href="/react-native-nitro-h3/showcase/"
  />
  <LinkCard
    title="Migrating from h3-js"
    description="Every change at the call site, before and after."
    href="/react-native-nitro-h3/migrating-from-h3-js/"
  />
  <LinkCard
    title="API reference"
    description="Every exported function by category."
    href="/react-native-nitro-h3/api/"
  />
</CardGrid>
