/** Points the border of a panel takes on every edge. */
export const PANEL_BORDER = 1

/** Points of padding between a panel's border and its contents, top and bottom. */
export const PANEL_PADDING = 12

/** Points between the head of a panel and the body that folds under it. */
export const PANEL_GAP = 8
